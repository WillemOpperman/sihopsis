.page-header
  %h3
    Campaign_schedule_placement_discount_lines
%ul.nav.nav-tabs
  %li.active
    %a{:href => '#list', :data => {:toggle => :tab}}
      = icon_tag('list-alt')
      = t(:list, :scope => :tabs)
  %li
    %a{:href => '#new', :data => {:toggle => :tab}}
      = icon_tag('plus')
      = t(:add_new, :scope => :tabs)

  %li.dropdown.pull-right
    %a.dropdown-toggle{:data => {:toggle => "dropdown", :target => "#"}}
      = icon_tag('arrow-circle-o-down')
      = t(:download, :scope => :tabs)
      %b.caret
    %ul.dropdown-menu.pull-right
      %li
        = link_to t(:csv, :scope => :formats), campaign_schedule_placement_discount_lines_path(:format => :csv)
      %li
        = link_to t(:xls, :scope => :formats), campaign_schedule_placement_discount_lines_path(:format => :xls)

.tab-content
  #list.tab-pane.active
    = render :partial => 'shared/grid_search', :locals => { :url => campaign_schedule_placement_discount_lines_path }

    %table.table.table-striped.table-bordered.table-condensed
      %thead
        %tr
          %th
            Uid
          %th
            Code
          %th
            Campaign schedule placement
          %th
            Media discount surcharge type
          %th
            Media discount surcharge type client deal
          %th
            Discount percent
          %th
            Discount amount
          %th
            Discount to client percent
          %th
            Discount to client amount
          %th
            Is default
          %th
            Eis sync date
          %th
            Apply after
          %th.button3
      %tbody
        - @campaign_schedule_placement_discount_lines.each do |campaign_schedule_placement_discount_line|
          %tr
            %td
              = campaign_schedule_placement_discount_line.uid
            %td
              = campaign_schedule_placement_discount_line.code
            %td
              = campaign_schedule_placement_discount_line.campaign_schedule_placement
            %td
              = campaign_schedule_placement_discount_line.media_discount_surcharge_type
            %td
              = campaign_schedule_placement_discount_line.media_discount_surcharge_type_client_deal
            %td
              = campaign_schedule_placement_discount_line.discount_percent
            %td
              = campaign_schedule_placement_discount_line.discount_amount
            %td
              = campaign_schedule_placement_discount_line.discount_to_client_percent
            %td
              = campaign_schedule_placement_discount_line.discount_to_client_amount
            %td
              = campaign_schedule_placement_discount_line.is_default
            %td
              = campaign_schedule_placement_discount_line.eis_sync_date
            %td
              = campaign_schedule_placement_discount_line.apply_after
            %td
              .btn-group
                = link_to campaign_schedule_placement_discount_line, :title => t(:show, :scope => :messages), :class => [:btn, :as_tooltip] do
                  = icon_tag('eye')
                = link_to edit_campaign_schedule_placement_discount_line_path(campaign_schedule_placement_discount_line), :title => t(:edit, :scope => :messages), :class => [:btn, :as_tooltip] do
                  = icon_tag('pencil')
                = link_to campaign_schedule_placement_discount_line, :method => :delete, :data => { :confirm => t(:delete_confirmation, :scope => :messages) }, :title => t(:delete, :scope => :messages), :class => [:btn, :as_tooltip] do
                  = icon_tag('times')

    .pull-right
      = paginate(@campaign_schedule_placement_discount_lines)

  #new.tab-pane
    = render 'form'
