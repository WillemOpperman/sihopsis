.page-header
  %h3
    Supplier_order_requests
%ul.nav.nav-tabs
  %li.active
    %a{:href => '#list', :data => {:toggle => :tab}}
      = icon_tag('list-alt')
      = t(:list, :scope => :tabs)
  %li
    %a{:href => '#new', :data => {:toggle => :tab}}
      = icon_tag('plus')
      = t(:add_new, :scope => :tabs)

  %li.dropdown.pull-right
    %a.dropdown-toggle{:data => {:toggle => "dropdown", :target => "#"}}
      = icon_tag('arrow-circle-o-down')
      = t(:download, :scope => :tabs)
      %b.caret
    %ul.dropdown-menu.pull-right
      %li
        = link_to t(:csv, :scope => :formats), supplier_order_requests_path(:format => :csv)
      %li
        = link_to t(:xls, :scope => :formats), supplier_order_requests_path(:format => :xls)

.tab-content
  #list.tab-pane.active
    = render :partial => 'shared/grid_search', :locals => { :url => supplier_order_requests_path }

    %table.table.table-striped.table-bordered.table-condensed
      %thead
        %tr
          %th
            Uid
          %th
            Code
          %th
            Code external
          %th
            Company division
          %th
            Campaign schedule
          %th
            Supplier
          %th
            Media space
          %th
            Contact
          %th
            Communication status
          %th
            Creation source
          %th
            Revision no
          %th
            Order gross amount
          %th
            Order commission amount
          %th
            Order nett amount
          %th
            Tax type
          %th
            Tax gross amount
          %th
            Tax nett amount
          %th
            Eis sync date
          %th
            Order gross amount home
          %th
            Order commission amount home
          %th
            Order nett amount home
          %th
            Tax gross amount home
          %th
            Tax nett amount home
          %th
            Exchange rate
          %th
            Currency
          %th
            Status
          %th
            Order nett inc tax amount
          %th.button3
      %tbody
        - @supplier_order_requests.each do |supplier_order_request|
          %tr
            %td
              = supplier_order_request.uid
            %td
              = supplier_order_request.code
            %td
              = supplier_order_request.code_external
            %td
              = supplier_order_request.company_division
            %td
              = supplier_order_request.campaign_schedule
            %td
              = supplier_order_request.supplier
            %td
              = supplier_order_request.media_space
            %td
              = supplier_order_request.contact
            %td
              = supplier_order_request.communication_status
            %td
              = supplier_order_request.creation_source
            %td
              = supplier_order_request.revision_no
            %td
              = supplier_order_request.order_gross_amount
            %td
              = supplier_order_request.order_commission_amount
            %td
              = supplier_order_request.order_nett_amount
            %td
              = supplier_order_request.tax_type
            %td
              = supplier_order_request.tax_gross_amount
            %td
              = supplier_order_request.tax_nett_amount
            %td
              = supplier_order_request.eis_sync_date
            %td
              = supplier_order_request.order_gross_amount_home
            %td
              = supplier_order_request.order_commission_amount_home
            %td
              = supplier_order_request.order_nett_amount_home
            %td
              = supplier_order_request.tax_gross_amount_home
            %td
              = supplier_order_request.tax_nett_amount_home
            %td
              = supplier_order_request.exchange_rate
            %td
              = supplier_order_request.currency
            %td
              = supplier_order_request.status
            %td
              = supplier_order_request.order_nett_inc_tax_amount
            %td
              .btn-group
                = link_to supplier_order_request, :title => t(:show, :scope => :messages), :class => [:btn, :as_tooltip] do
                  = icon_tag('eye')
                = link_to edit_supplier_order_request_path(supplier_order_request), :title => t(:edit, :scope => :messages), :class => [:btn, :as_tooltip] do
                  = icon_tag('pencil')
                = link_to supplier_order_request, :method => :delete, :data => { :confirm => t(:delete_confirmation, :scope => :messages) }, :title => t(:delete, :scope => :messages), :class => [:btn, :as_tooltip] do
                  = icon_tag('times')

    .pull-right
      = paginate(@supplier_order_requests)

  #new.tab-pane
    = render 'form'
