.page-header
  %h3
    Campaign_schedule_placement_routing_addresses
%ul.nav.nav-tabs
  %li.active
    %a{:href => '#list', :data => {:toggle => :tab}}
      = icon_tag('list-alt')
      = t(:list, :scope => :tabs)
  %li
    %a{:href => '#new', :data => {:toggle => :tab}}
      = icon_tag('plus')
      = t(:add_new, :scope => :tabs)

  %li.dropdown.pull-right
    %a.dropdown-toggle{:data => {:toggle => "dropdown", :target => "#"}}
      = icon_tag('arrow-circle-o-down')
      = t(:download, :scope => :tabs)
      %b.caret
    %ul.dropdown-menu.pull-right
      %li
        = link_to t(:csv, :scope => :formats), campaign_schedule_placement_routing_addresses_path(:format => :csv)
      %li
        = link_to t(:xls, :scope => :formats), campaign_schedule_placement_routing_addresses_path(:format => :xls)

.tab-content
  #list.tab-pane.active
    = render :partial => 'shared/grid_search', :locals => { :url => campaign_schedule_placement_routing_addresses_path }

    %table.table.table-striped.table-bordered.table-condensed
      %thead
        %tr
          %th
            Uid
          %th
            Code
          %th
            Name
          %th
            Campaign schedule placement
          %th
            Routing address source type
          %th
            Routing address type
          %th
            Routing address
          %th
            Notes
          %th
            Eis sync date
          %th.button3
      %tbody
        - @campaign_schedule_placement_routing_addresses.each do |campaign_schedule_placement_routing_address|
          %tr
            %td
              = campaign_schedule_placement_routing_address.uid
            %td
              = campaign_schedule_placement_routing_address.code
            %td
              = campaign_schedule_placement_routing_address.name
            %td
              = campaign_schedule_placement_routing_address.campaign_schedule_placement
            %td
              = campaign_schedule_placement_routing_address.routing_address_source_type
            %td
              = campaign_schedule_placement_routing_address.routing_address_type
            %td
              = campaign_schedule_placement_routing_address.routing_address
            %td
              = campaign_schedule_placement_routing_address.notes
            %td
              = campaign_schedule_placement_routing_address.eis_sync_date
            %td
              .btn-group
                = link_to campaign_schedule_placement_routing_address, :title => t(:show, :scope => :messages), :class => [:btn, :as_tooltip] do
                  = icon_tag('eye')
                = link_to edit_campaign_schedule_placement_routing_address_path(campaign_schedule_placement_routing_address), :title => t(:edit, :scope => :messages), :class => [:btn, :as_tooltip] do
                  = icon_tag('pencil')
                = link_to campaign_schedule_placement_routing_address, :method => :delete, :data => { :confirm => t(:delete_confirmation, :scope => :messages) }, :title => t(:delete, :scope => :messages), :class => [:btn, :as_tooltip] do
                  = icon_tag('times')

    .pull-right
      = paginate(@campaign_schedule_placement_routing_addresses)

  #new.tab-pane
    = render 'form'
