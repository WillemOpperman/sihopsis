= simple_form_for(@media_target_alias, :html => { :class => 'form-horizontal' }) do |f|
  = f.error_messages

  .form-inputs
    = f.input :uid
    = f.input :code
    = f.input :name
    = f.association :media_target
    = f.association :creation_source

  .form-actions
    = f.button :wrapped,
               :delete => @media_target_alias,
               :cancel => media_target_aliases_path,
               :data => { :disable_with => t(:saving, :scope => :forms) }
